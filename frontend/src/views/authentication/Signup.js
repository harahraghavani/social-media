import React from 'react'
import Auth from '../../components/authentication/Auth'

const Signup = () => {
    return (
        <div>
            <Auth isSignup={true} />
        </div>
    )
}

export default Signup